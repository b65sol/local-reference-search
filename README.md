A rudimentary local document search engine based on TNTSearch

To install dependencies run: `composer install`

(Note there are some dependencies that are extraneous)

To add HTML files to the index use add-to-index.php
